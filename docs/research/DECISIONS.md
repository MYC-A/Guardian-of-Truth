# Decisions — 2026-09-19

| Decision | Basis | Revision gate |
|---|---|---|
| KEEP the existing `--backend none` CLI as an offline competition baseline | It processed 46 viewed rows in 0.395 s at `923bb445` and produced TP12 FP0 FN11 TN23; it also accepted fresh unseen CSV. `scripts/predict.py` uses this code. | Replace only after an independent, measured gain and valid offline entrypoint. |
| REJECT implicit closed catalog and `additionalProperties=false` as formal premises | `4639b46` source audit found no closure statement; 7 of 10 former fix2 TP depended on invented closure. | Explicit source-backed closure premise, checker re-derivation, adversarial test. |
| REJECT direct promotion of historical FullArch N5 | The replay reproduced 2 FP despite valid certificates. Both came from a compiler that discarded a rule's unrepresentable guard and exception. The final frontend also depends on frozen Mistral Phi. | Case-level source-to-verdict trace, sound lowering, a hidden-input offline frontend, and independent F1 gain. |
| KEEP the conservative RuleIR lowering fix | Commit `36d4b58` refuses to emit obligations when a condition, exception, temporal guard, explicit unresolved element, or complete interpretation space cannot be preserved. The same N5 replay changed from TP4/FP2 to TP3/FP0; both FP became `UNRESOLVED`, along with one former TP. | Broaden representation only with source-equivalent lowering tests and no false certification. |
| REJECT neural target binding as a formal premise | The remaining three N5 positives used cross-encoder mappings, including unrelated retail policy text mapped to `return_delivered_order_items`. With exact identity required, the clean 46-row replay is entirely unresolved. | A source span that explicitly names the action, a deterministic identity transform, and checker-visible provenance. Neural similarity may remain a probabilistic contest feature. |
| KEEP conservative ASP value/id serialization | Clingo rejects decimal syntax and interprets hyphens in unquoted identifiers as subtraction. The correction preserves integral numbers, represents non-integral floats as typed nonnumeric values, and quotes opaque fact IDs. | Add exact rational/decimal arithmetic only with a tested scale representation and type-preserving comparisons. |
| DEFER new formal engines for generic inputs | Clingo/s(CASP)/Drools already 43/43 on neutral formal data; NL extraction and completeness dominate. | Specific policy or numeric case where current engine is representationally inadequate and a new engine resolves it without FP. |
| PROBE Granite Guardian function-call signal separately | Eleven baseline FN include many tool/argument cases; official Granite card supports function-call evaluation. | License/import/VRAM, three fresh smoke cases, then independent balanced set with TP/FP and latency. Score remains probabilistic. |
| KEEP the Granite competition adapter as an isolated experiment boundary | Commit `08e5394` consumes only `id,prompt,response`, selects the last user message and assistant tool calls, carries source spans, and returns explicit `unavailable` for missing/ambiguous/truncated inputs. It never asserts catalog closure. | Real local model smoke and independent TP/FP measurements before any production routing. |
| KEEP NLI only as a cheap claim diagnostic | Cached DeBERTa-base processed the 16 frozen claim pairs with 8 entailment and 8 contradiction predictions in under one second total. Exact TP/FP remains unscored until prediction records are recovered after the SSH interruption. | Score the sealed records against separate gold, then test on claim-only independent data; never apply it to tool/policy errors as proof. |
| DEFER broad Docker claims | Docker is absent locally and on the prepared server. Isolated `pip install ".[data]"` and fresh CSV inference pass, so the Python packaging path is verified; container build, cold time and image size remain unknown. | Run an isolated Docker build and cold fresh-input inference where Docker is available. |
