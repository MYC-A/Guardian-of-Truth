# Native factual v5 integration experiment v1

Status: FROZEN_NOT_RUN. Candidate commit:
`f36bdf54fecd90c2cfc9829ef2fef7b804a9b690`; source/config/input freeze:
`outputs/vnext/native_factual_v5_dev_v1_freeze.json`. No model requests have
been admitted for this experiment. Twelve explicit executable-fixture traces test the real
C2 ten-pass frontend, source method-meaning pass, exhaustive entity binding,
T1 history queries and independent native receipts together. This is a
post-implementation controlled development benchmark, not a blind holdout, a
new pre-fix test or a whole-Core quality measurement. It complements, rather than
replaces, the existing 34 typed-query results and 19 controlled-backend tests.

The source reference generates events from a pinned, documented executable
fixture. It does not label candidate outputs during inference. Case IDs,
reference truth, latent state and failure-family names do not enter model inputs.
The fixture query is used by the application adapter only; C2 gets the target
response and the method pass gets the authoritative catalogue/source context.
Every deterministic span remains in the output, including unsupported spans.

Cases cover history after undo, never/absence with complete versus partial
history, timeout followed by observation, user versus assistant actions, noop
versus versioned new mutation, literal current aliases and delete/restore.
They use explicit fixture semantics, not guessed real-provider tool effects.

The independent fixture's method name selector uses latest observed aliases,
whereas the native Binder also preserves historical aliases. A retired-name
method claim would therefore compare different query meanings. It is excluded
from this network benchmark before candidate inference; historical aliases
remain covered separately by the typed and controlled-backend tests. This
boundary is not a candidate fix or a changed previously frozen result.

Before inference, commit and freeze executable sources, fixture/spec input hashes,
provider and transport, prompts persisted before every request, metrics and
adapter boundary. Prediction files and their full seal precede reference joins.
After evaluation, preserve the full per-case failure audit before any new-version
fix. Interrupted admitted requests are UNKNOWN and never resent automatically.

Only conditional primitive truth is measured. UNKNOWN, conflicting span values,
unlowered claims and causal obligations never become a safe zero. Semantic
completeness remains empirical/open; validated receipts are not evidence of
closed unrestricted NL meaning, ERROR, NO_ERROR or global safety. Report
conditional truth accuracy/distribution, all receipts, exact telemetry and
per-case failures. Whole-Core resolution and binary F1 are NOT_EVALUATED/null.

The runner rejects network admission until the preceding 104-case frozen Policy
report and linked per-case audit exist. No additional network job is started
while Policy is active. Goal v2 and all earlier frozen sources remain immutable;
Goal v3 still follows Policy completion and audit, as requested.
