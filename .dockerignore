.git
.env
.env.*
outputs
valid.parquet
tests
docs
__pycache__
*.egg-info
model
# §26 cold-build: the image carries ONLY inference entrypoints plus the one
# frozen module the B4h engine imports (scripts/evaluate_vnext_c_alr_reimpl.py,
# the frozen historical H0 source; zero hardcoding findings in the §26 scan).
# All other benchmark/research scripts contain case ids / gold-derived freeze
# data (e.g. freeze_v4_claim_set.py) and must never enter the submission image.
scripts/*
!scripts/predict.py
!scripts/predict_e2e_competition.py
!scripts/evaluate_vnext_c_alr_reimpl.py
